# HookFlow Deployment

HookFlow's active development and app network is Ethereum Sepolia (chain ID `11155111`).

Use [SEPOLIA_DEPLOYMENT.md](SEPOLIA_DEPLOYMENT.md) for the canonical addresses, environment variables, deploy commands, and the atomic pool-creation workflow.

The former X Layer deployment is retained only as historical proof in [MAINNET_DEPLOYMENT.md](MAINNET_DEPLOYMENT.md); it is not used by the current frontend or deployment defaults.
